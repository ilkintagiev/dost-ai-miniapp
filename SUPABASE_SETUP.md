# 📋 Инструкция: Настройка Supabase Database

## Шаг 1: Создай таблицы в Supabase

1. Открой свой проект Supabase: https://unogiolsuowfijokmfqb.supabase.co
2. В левом меню нажми **"SQL Editor"** (иконка </> )
3. Нажми **"New query"**
4. Скопируй весь код из файла `database-schema.sql`
5. Вставь в редактор
6. Нажми **"Run"** (или Ctrl/Cmd + Enter)

✅ Должно появиться сообщение "Success. No rows returned"

---

## Шаг 2: Проверь таблицы

1. В левом меню нажми **"Table Editor"**
2. Должны появиться 3 таблицы:
   - **products** (товары)
   - **orders** (заказы)
   - **users** (пользователи)

---

## Шаг 3: Создай Storage для изображений

1. В левом меню нажми **"Storage"**
2. Нажми **"Create a new bucket"**
3. Заполни:
   - **Name**: `product-images`
   - **Public bucket**: ✅ (включи!)
4. Нажми **"Create bucket"**

---

## Шаг 4: Настрой Storage политики

1. Нажми на bucket `product-images`
2. Перейди в **"Policies"**
3. Нажми **"New Policy"**
4. Выбери **"For full customization"**
5. Создай политику для загрузки:

```sql
CREATE POLICY "Anyone can upload images"
ON storage.objects FOR INSERT
WITH CHECK (bucket_id = 'product-images');
```

6. Создай политику для чтения:

```sql
CREATE POLICY "Anyone can view images"
ON storage.objects FOR SELECT
USING (bucket_id = 'product-images');
```

---

## Шаг 5: Проверь данные

1. Перейди в **"Table Editor"** → **"products"**
2. Должны быть 6 товаров (sample data)
3. Если нет - запусти INSERT запрос из `database-schema.sql` еще раз

---

## ✅ Готово!

Теперь база данных настроена и готова к использованию!

**Что дальше:**
- Обнови страницу Mini App
- Товары будут загружаться из Supabase
- Заказы будут сохраняться в базу

---

## 🔧 Если что-то не работает:

**Проблема:** Таблицы не создались
- Проверь что SQL запрос выполнился без ошибок
- Попробуй запустить по частям (сначала CREATE TABLE, потом остальное)

**Проблема:** Нет прав на Storage
- Убедись что bucket `product-images` публичный
- Проверь политики доступа

**Проблема:** Sample data не добавились
- Запусти только INSERT часть из SQL файла
- Или добавь товары вручную через Table Editor
